name: Chainguard Image Diff

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # needed for chainguard-dev/setup-chainctl OIDC exchange
  issues: write

env:
  IMAGE: cgr.dev/datatomix.com/python

jobs:
  diff:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Install chainctl ────────────────────────────────────────────────────
      # Binary filename is chainctl_linux_x86_64 (not amd64).
      - name: Install chainctl
        run: |
          curl -sSfL https://dl.enforce.dev/chainctl/latest/chainctl_linux_x86_64 \
            -o /usr/local/bin/chainctl
          chmod +x /usr/local/bin/chainctl
          chainctl version

      # ── Authenticate ────────────────────────────────────────────────────────
      # Generate credentials on your laptop:
      #   chainctl auth configure-docker --pull-token --parent datatomix.com
      # Store the printed username → CHAINGUARD_USERNAME secret
      # Store the printed password → CHAINGUARD_TOKEN secret
      #
      # docker/login-action handles registry pulls (imagetools inspect).
      # chainctl auth login --identity-token handles the Chainguard API
      # (SBOM + vuln data fetched by chainctl images diff) — fully headless.
      - name: Log in to Chainguard registry
        uses: docker/login-action@v3
        with:
          registry: cgr.dev
          username: ${{ secrets.CHAINGUARD_USERNAME }}
          password: ${{ secrets.CHAINGUARD_TOKEN }}

      # ── Authenticate chainctl ───────────────────────────────────────────────
      # Uses GitHub Actions OIDC to exchange for a Chainguard token — fully
      # headless. Requires a Chainguard assumable identity bound to this repo.
      #
      # One-time setup on your laptop:
      #   1. chainctl iam identities create github <name> \
      #        --github-repo=<org>/<repo> \
      #        --github-ref=refs/heads/main \
      #        --role=viewer
      #   2. Copy the printed identity ID → secret CHAINGUARD_IDENTITY
      - name: Authenticate chainctl
        uses: chainguard-dev/setup-chainctl@v0.2.2
        with:
          identity: ${{ secrets.CHAINGUARD_IDENTITY }}

      # ── Resolve the current live digest ────────────────────────────────────
      - name: Resolve latest digest
        id: latest
        run: |
          DIGEST=$(docker buildx imagetools inspect "${{ env.IMAGE }}:latest" \
            --format '{{.Manifest.Digest}}')
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "Latest: ${DIGEST}"

      # ── Load the previously pinned digest from the Dockerfile ───────────────
      - name: Read pinned digest from Dockerfile
        id: pinned
        run: |
          # Handles both:
          #   FROM image@sha256:...
          #   FROM image:tag@sha256:...
          DIGEST=$(grep -m1 '^FROM' Dockerfile | grep -o 'sha256:[a-f0-9]\{64\}')
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "Pinned: ${DIGEST}"

      # ── Compare ─────────────────────────────────────────────────────────────
      - name: Check for update
        id: changed
        run: |
          if [[ "${{ steps.pinned.outputs.digest }}" == "${{ steps.latest.outputs.digest }}" ]]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Image is up to date — nothing to do."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "New digest available!"
          fi

      # ── Install Grype ──────────────────────────────────────────────────────
      # chainctl images diff uses Grype internally to scan vulnerabilities.
      - name: Install Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh \
            | sh -s -- -b /usr/local/bin

      # ── Run chainctl images diff ────────────────────────────────────────────
      - name: Run chainctl images diff
        id: diff
        if: steps.changed.outputs.changed == 'true'
        run: |
          OLD="${{ env.IMAGE }}@${{ steps.pinned.outputs.digest }}"
          NEW="${{ env.IMAGE }}@${{ steps.latest.outputs.digest }}"

          echo "Diffing:"
          echo "  old: ${OLD}"
          echo "  new: ${NEW}"

          # -o markdown produces a human-friendly table ready for GitHub issues
          DIFF=$(chainctl images diff "${OLD}" "${NEW}" -o markdown 2>&1) || true

          {
            echo 'result<<EOF'
            echo "${DIFF}"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      # ── Write step summary ──────────────────────────────────────────────────
      - name: Write job summary
        if: steps.changed.outputs.changed == 'true'
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<SUMMARY
          ## 🔄 New Chainguard Python build available

          | | |
          |---|---|
          | **Image** | \`${{ env.IMAGE }}\` |
          | **Pinned digest** | \`${{ steps.pinned.outputs.digest }}\` |
          | **Latest digest** | \`${{ steps.latest.outputs.digest }}\` |

          ${{ steps.diff.outputs.result }}
          SUMMARY

      # ── Open or update a GitHub Issue ──────────────────────────────────────
      - name: Open or update GitHub Issue
        if: steps.changed.outputs.changed == 'true'
        uses: actions/github-script@v7
        env:
          PINNED: ${{ steps.pinned.outputs.digest }}
          LATEST: ${{ steps.latest.outputs.digest }}
          DIFF:   ${{ steps.diff.outputs.result }}
        with:
          script: |
            const image  = process.env.IMAGE  || '${{ env.IMAGE }}';
            const pinned = process.env.PINNED;
            const latest = process.env.LATEST;
            const diff   = process.env.DIFF;

            const title = `[chainguard] New python build: ${latest.slice(0, 19)}`;

            const body = [
              `## 🔄 New Chainguard Python build`,
              ``,
              `| | |`,
              `|---|---|`,
              `| **Image** | \`${image}\` |`,
              `| **Current digest** | \`${pinned}\` |`,
              `| **New digest** | \`${latest}\` |`,
              ``,
              `### Package diff`,
              ``,
              diff,
              ``,
              `### Update your Dockerfile`,
              ``,
              '```bash',
              `sed -i 's|${pinned}|${latest}|g' Dockerfile`,
              '```',
            ].join('\n');

            const { data: issues } = await github.rest.issues.listForRepo({
              owner:  context.repo.owner,
              repo:   context.repo.repo,
              state:  'open',
              labels: 'chainguard-diff',
            });

            const existing = issues.find(i => i.title === title);
            if (existing) {
              await github.rest.issues.createComment({
                owner:        context.repo.owner,
                repo:         context.repo.repo,
                issue_number: existing.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner:  context.repo.owner,
                repo:   context.repo.repo,
                title,
                body,
                labels: ['chainguard-diff'],
              });
            }
